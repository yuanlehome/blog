---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { getReadingTime } from '../lib/content/readingTime';
import { resolveAssetUrl } from '../lib/site/assetUrl';
import TableOfContents from '../components/TableOfContents.astro';
import MobileToc from '../components/MobileToc.astro';
import PrevNext from '../components/PrevNext.astro';
import RelatedPosts from '../components/RelatedPosts.astro';
import { findPrevNext, findRelated, getPublishedPosts } from '../lib/content/posts';
import Giscus from '../components/Giscus.astro';
import ReadingProgress from '../components/ReadingProgress.astro';
import FloatingActionStack from '../components/FloatingActionStack.astro';
import ScrollUpTopButton from '../components/ScrollUpTopButton.astro';
import { getDisplayDates } from '../lib/content/dates';
import { getLayoutConfig } from '../config/loaders';
import { alignToTextClass, alignToJustifyClass, alignToItemsClass } from '../lib/ui/alignment';
import Tag from '../components/Tag.astro';
import { buildTagIndex } from '../lib/content/tags';

export async function getStaticPaths() {
  const posts = await getPublishedPosts();
  return posts.map((post) => ({
    params: { slug: post.slug },
    props: { post, all: posts },
  }));
}

const { post, all } = Astro.props;
const { Content, headings } = await post.render();
const tocHeadings = headings.filter((heading) => heading.depth >= 2 && heading.depth <= 3);
const hasToc = tocHeadings.length > 0;
const stats = getReadingTime(post.body);
const { prev, next } = findPrevNext(all, post.slug);
const related = findRelated(all, post);
const commentsEnabled = post.data.comments ?? true;
const dates = getDisplayDates(post.data);
const effectiveCover = post.data.cover;
const hasCover = Boolean(effectiveCover);
const postTags = post.data.tags || [];
const { tagMap } = buildTagIndex(all);
const tagSlugs = postTags
  .map((tagName) => {
    // Find the slug for this tag name
    const entry = Object.entries(tagMap).find(([_, data]) => data.name === tagName);
    return entry ? { name: tagName, slug: entry[0] } : null;
  })
  .filter(Boolean) as { name: string; slug: string }[];

// Get post meta alignment from config
const layoutConfig = getLayoutConfig();
const postMetaAlign = layoutConfig.alignment.postMetaAlign;
const metaTextClass = alignToTextClass(postMetaAlign);
const metaJustifyClass = alignToJustifyClass(postMetaAlign);

// Get TOC position from config
const tocPosition = layoutConfig.toc.position;
---

<Layout title={post.data.title} image={resolveAssetUrl(effectiveCover)}>
  <ReadingProgress />
  <Header />
  <main class="container mx-auto px-4 pb-8 pt-8 lg:pt-0 max-w-6xl">
    <div
      class={`flex flex-col gap-6 ${
        hasToc && tocPosition === 'left'
          ? 'lg:mx-auto lg:grid lg:w-fit lg:max-w-full lg:grid-cols-[20rem_minmax(0,54rem)] lg:items-start lg:gap-8'
          : hasToc && tocPosition === 'right'
            ? 'lg:mx-auto lg:grid lg:w-fit lg:max-w-full lg:grid-cols-[minmax(0,54rem)_20rem] lg:items-start lg:gap-8'
            : 'lg:mx-auto lg:w-full lg:max-w-[54rem]'
      }`}
      data-post-layout
    >
      {hasToc && tocPosition === 'left' && <TableOfContents headings={tocHeadings} />}
      <article
        class="prose dark:prose-invert max-w-none prose-img:rounded-xl prose-img:cursor-zoom-in min-w-0"
        data-article
      >
        <header class="mb-8 not-prose">
          <div
            class={`flex flex-col gap-6 ${
              hasCover
                ? 'md:grid md:grid-cols-[minmax(0,1fr)_220px] md:items-start md:gap-8'
                : 'md:grid md:grid-cols-1'
            }`}
          >
            <div class={`flex flex-col gap-4 ${hasCover ? 'md:h-full' : ''}`}>
              <h1 class={`text-3xl md:text-4xl font-bold leading-tight ${metaTextClass}`}>
                {post.data.title}
              </h1>
              <div
                class={`text-zinc-500 text-sm flex ${alignToItemsClass(postMetaAlign)} ${metaJustifyClass} gap-3 flex-wrap ${
                  hasCover ? 'md:mt-auto' : ''
                }`}
              >
                {
                  dates.publishedLabel && (
                    <time
                      class="flex items-center gap-1"
                      datetime={dates.published?.toISOString()}
                      title={`发布于 ${dates.publishedLabel}`}
                    >
                      <span aria-hidden="true">📅</span>
                      <span>
                        发布于 <span class="font-medium">{dates.publishedLabel}</span>
                      </span>
                    </time>
                  )
                }
                {dates.publishedLabel && dates.updatedLabel && <span aria-hidden="true">·</span>}
                {
                  dates.updatedLabel && (
                    <time
                      class="flex items-center gap-1"
                      datetime={dates.updated?.toISOString()}
                      title={`更新于 ${dates.updatedLabel}`}
                    >
                      <span aria-hidden="true">🔄</span>
                      <span>
                        更新于 <span class="font-medium">{dates.updatedLabel}</span>
                      </span>
                    </time>
                  )
                }
                <div class="flex items-center gap-1">
                  <span aria-hidden="true">✍️</span>
                  {stats.wordCount} 字
                </div>
                <div class="flex items-center gap-1" title={`${stats.wordCount} words`}>
                  <span aria-hidden="true">⏱️</span>
                  {stats.text}
                </div>
              </div>

              <div class="flex flex-wrap items-center gap-2" data-testid="post-share-tags-row">
                {
                  tagSlugs.length > 0 && (
                    <div class="flex flex-wrap gap-2 items-center" data-testid="post-tags">
                      <span class="text-zinc-500 text-sm flex items-center gap-1">
                        <span aria-hidden="true">🏷️</span>
                        <span>标签：</span>
                      </span>
                      {tagSlugs.map((tag) => (
                        <Tag name={tag.name} slug={tag.slug} size="sm" />
                      ))}
                    </div>
                  )
                }
                <div class="inline-flex items-center gap-2" data-testid="post-share-action">
                  <span class="text-zinc-500 text-sm" aria-hidden="true"> 📤 </span>
                  <button
                    type="button"
                    class="inline-flex items-center rounded-full border border-pink-200 bg-pink-50 px-2 py-1 text-xs font-medium text-pink-700 transition-colors duration-200 hover:border-pink-300 hover:bg-pink-100 dark:border-pink-900 dark:bg-pink-950 dark:text-pink-300 dark:hover:border-pink-700 dark:hover:bg-pink-900 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-pink-500 focus-visible:ring-offset-2 dark:focus-visible:ring-offset-zinc-900"
                    data-share-open
                    aria-haspopup="dialog"
                    aria-controls="article-share-dialog"
                  >
                    分享文章
                  </button>
                </div>
              </div>
            </div>

            {
              hasCover && (
                <figure
                  class="hidden md:block md:justify-self-end overflow-hidden rounded-xl border border-zinc-200/80 shadow-md dark:border-zinc-800/80 w-full max-w-[320px] md:max-w-[220px] md:min-w-[160px]"
                  data-post-cover
                >
                  <img
                    src={resolveAssetUrl(effectiveCover)}
                    alt={post.data.title}
                    class="aspect-[4/3] w-full object-cover transition duration-200"
                    loading="eager"
                    decoding="async"
                  />
                </figure>
              )
            }

            {
              post.data.source_url && (
                <div
                  class={`w-full rounded-lg border border-sky-200 bg-sky-50 px-4 py-3 dark:border-sky-900 dark:bg-sky-950 ${
                    hasCover ? 'md:col-span-2' : ''
                  }`}
                >
                  <p class="text-sm text-zinc-700 dark:text-zinc-300 break-words">
                    <span class="font-semibold">📝 转载 / 引用来源：</span>
                    {post.data.source_author && (
                      <span>
                        作者 <span class="font-medium">{post.data.source_author}</span> ·{' '}
                      </span>
                    )}
                    <a
                      href={post.data.source_url}
                      target="_blank"
                      rel="noopener noreferrer"
                      class="text-sky-600 underline hover:text-sky-700 dark:text-sky-400 dark:hover:text-sky-300"
                    >
                      查看原文
                    </a>
                  </p>
                </div>
              )
            }
          </div>
        </header>

        <Content />

        <PrevNext prev={prev} next={next} />
        <RelatedPosts posts={related} />
        {commentsEnabled && <Giscus lang="zh-CN" loading="lazy" />}
        <div id="page-bottom-anchor" aria-hidden="true" style="height: 1px;"></div>
      </article>
      {hasToc && tocPosition === 'right' && <TableOfContents headings={tocHeadings} />}
    </div>
    {hasToc && <MobileToc headings={tocHeadings} showTrigger={false} />}
    <FloatingActionStack enableToc={hasToc} />
    <ScrollUpTopButton targetSelector="[data-article]" />
  </main>
  <Footer />
</Layout>

<dialog
  id="article-image-zoom-dialog"
  class="fixed inset-0 m-0 h-screen w-screen max-h-none max-w-none touch-none border-0 bg-black/85 p-0 backdrop:bg-black/85 [&[open]]:grid [&[open]]:place-items-center"
  aria-label="图片预览"
>
  <button
    type="button"
    data-image-zoom-close
    class="absolute right-4 top-4 z-10 inline-flex h-10 w-10 items-center justify-center rounded-full bg-black/55 text-2xl text-white transition hover:bg-black/70 focus:outline-none focus:ring-2 focus:ring-white"
    aria-label="关闭预览"
  >
    ×
  </button>
  <img
    data-image-zoom-preview
    alt=""
    draggable="false"
    class="max-h-[90vh] max-w-[90vw] touch-none select-none rounded-lg bg-zinc-100 p-2 object-contain shadow-2xl transition-transform duration-150 ease-out will-change-transform dark:bg-zinc-800"
    loading="eager"
    decoding="async"
  />
  <div
    class="pointer-events-none absolute bottom-4 left-1/2 z-10 -translate-x-1/2 rounded-full bg-black/55 px-3 py-2"
  >
    <div class="pointer-events-auto flex items-center gap-2 text-sm text-white">
      <button
        type="button"
        data-image-zoom-out
        class="inline-flex h-8 w-8 items-center justify-center rounded-full border border-white/35 hover:bg-white/10"
        aria-label="缩小"
      >
        −
      </button>
      <span data-image-zoom-level class="min-w-14 text-center tabular-nums">100%</span>
      <button
        type="button"
        data-image-zoom-in
        class="inline-flex h-8 w-8 items-center justify-center rounded-full border border-white/35 hover:bg-white/10"
        aria-label="放大"
      >
        +
      </button>
    </div>
  </div>
</dialog>

<dialog
  id="article-share-dialog"
  class="w-[min(92vw,640px)] rounded-2xl border border-zinc-200 bg-white p-0 text-zinc-900 shadow-2xl backdrop:bg-black/50 dark:border-zinc-800 dark:bg-zinc-900 dark:text-zinc-100"
  aria-label="分享文章"
>
  <div
    class="flex items-center justify-between border-b border-zinc-200 px-5 py-4 dark:border-zinc-800"
  >
    <h2 class="text-lg font-semibold">分享文章</h2>
    <button
      type="button"
      data-share-close
      class="inline-flex h-9 w-9 items-center justify-center rounded-full text-zinc-500 transition hover:bg-zinc-100 hover:text-zinc-700 dark:text-zinc-400 dark:hover:bg-zinc-800 dark:hover:text-zinc-200"
      aria-label="关闭分享窗口"
    >
      ×
    </button>
  </div>
  <div class="space-y-5 px-5 py-4">
    <section class="space-y-2">
      <div class="flex items-center justify-between gap-3">
        <h3 class="text-sm font-medium text-zinc-700 dark:text-zinc-300">Markdown 链接</h3>
        <button
          type="button"
          class="rounded-md border border-zinc-300 px-3 py-1.5 text-sm transition hover:border-zinc-400 hover:bg-zinc-100 dark:border-zinc-700 dark:hover:border-zinc-500 dark:hover:bg-zinc-800"
          data-share-copy="markdown"
        >
          复制
        </button>
      </div>
      <pre
        class="overflow-x-hidden whitespace-pre-wrap break-all rounded-lg bg-zinc-100 px-3 py-2 text-sm text-zinc-700 dark:bg-zinc-800 dark:text-zinc-200"
        data-share-text="markdown">
      </pre>
    </section>
    <section class="space-y-2">
      <div class="flex items-center justify-between gap-3">
        <h3 class="text-sm font-medium text-zinc-700 dark:text-zinc-300">标题 + 链接</h3>
        <button
          type="button"
          class="rounded-md border border-zinc-300 px-3 py-1.5 text-sm transition hover:border-zinc-400 hover:bg-zinc-100 dark:border-zinc-700 dark:hover:border-zinc-500 dark:hover:bg-zinc-800"
          data-share-copy="plain"
        >
          复制
        </button>
      </div>
      <pre
        class="overflow-x-hidden whitespace-pre-wrap break-all rounded-lg bg-zinc-100 px-3 py-2 text-sm text-zinc-700 dark:bg-zinc-800 dark:text-zinc-200"
        data-share-text="plain">
      </pre>
    </section>
    <p
      data-share-status
      class="min-h-5 text-sm text-emerald-600 dark:text-emerald-400"
      aria-live="polite"
    >
    </p>
  </div>
</dialog>

<script>
  const articleRoot = document.querySelector('[data-article]') as HTMLElement | null;
  const shareDialog = document.querySelector('#article-share-dialog') as HTMLDialogElement | null;
  const shareOpenButton = document.querySelector('[data-share-open]') as HTMLButtonElement | null;
  const shareCloseButton = shareDialog?.querySelector(
    '[data-share-close]',
  ) as HTMLButtonElement | null;
  const shareMarkdownText = shareDialog?.querySelector(
    '[data-share-text="markdown"]',
  ) as HTMLElement | null;
  const sharePlainText = shareDialog?.querySelector(
    '[data-share-text="plain"]',
  ) as HTMLElement | null;
  const shareStatus = shareDialog?.querySelector('[data-share-status]') as HTMLElement | null;
  const shareMarkdownCopyButton = shareDialog?.querySelector(
    '[data-share-copy="markdown"]',
  ) as HTMLButtonElement | null;
  const sharePlainCopyButton = shareDialog?.querySelector(
    '[data-share-copy="plain"]',
  ) as HTMLButtonElement | null;

  const pageTitle = document.title.trim();
  const canonicalUrl =
    (document.querySelector('link[rel="canonical"]') as HTMLLinkElement | null)?.href ||
    window.location.href;
  const shareTexts = {
    markdown: `[${pageTitle}](${canonicalUrl})`,
    plain: `${pageTitle}\n${canonicalUrl}`,
  };
  let lockedScrollY = 0;

  const lockBodyScroll = () => {
    if (document.body.dataset.scrollLock === 'true') return;
    lockedScrollY = window.scrollY;
    document.body.style.position = 'fixed';
    document.body.style.top = `-${lockedScrollY}px`;
    document.body.style.left = '0';
    document.body.style.right = '0';
    document.body.style.width = '100%';
    document.body.style.overflow = 'hidden';
    document.body.dataset.scrollLock = 'true';
  };

  const unlockBodyScroll = () => {
    if (document.body.dataset.scrollLock !== 'true') return;
    const html = document.documentElement;
    const previousScrollBehavior = html.style.scrollBehavior;
    html.style.scrollBehavior = 'auto';
    document.body.style.position = '';
    document.body.style.top = '';
    document.body.style.left = '';
    document.body.style.right = '';
    document.body.style.width = '';
    document.body.style.overflow = '';
    delete document.body.dataset.scrollLock;
    window.scrollTo({ top: lockedScrollY, left: 0, behavior: 'auto' });
    html.style.scrollBehavior = previousScrollBehavior;
  };

  if (shareMarkdownText) {
    shareMarkdownText.textContent = shareTexts.markdown;
  }
  if (sharePlainText) {
    sharePlainText.textContent = shareTexts.plain;
  }

  const updateShareStatus = (message: string) => {
    if (!shareStatus) return;
    shareStatus.textContent = message;
  };

  const copyShareText = async (format: keyof typeof shareTexts) => {
    try {
      await navigator.clipboard.writeText(shareTexts[format]);
      updateShareStatus('已复制到剪贴板');
    } catch {
      updateShareStatus('复制失败，请手动复制文本');
    }
  };

  const closeShareDialog = () => {
    if (!shareDialog?.open) return;
    shareDialog.close();
    updateShareStatus('');
  };

  if (shareDialog && typeof shareDialog.showModal === 'function') {
    shareOpenButton?.addEventListener('click', () => {
      shareDialog.showModal();
      lockBodyScroll();
      updateShareStatus('');
    });

    shareCloseButton?.addEventListener('click', closeShareDialog);
    shareMarkdownCopyButton?.addEventListener('click', async () => {
      await copyShareText('markdown');
    });
    sharePlainCopyButton?.addEventListener('click', async () => {
      await copyShareText('plain');
    });

    shareDialog.addEventListener('click', (event) => {
      if (event.target === shareDialog) {
        closeShareDialog();
      }
    });
    shareDialog.addEventListener('cancel', () => {
      updateShareStatus('');
    });
    shareDialog.addEventListener('close', () => {
      unlockBodyScroll();
    });
  }

  const dialog = document.querySelector('#article-image-zoom-dialog') as HTMLDialogElement | null;
  const preview = dialog?.querySelector('[data-image-zoom-preview]') as HTMLImageElement | null;
  const closeButton = dialog?.querySelector('[data-image-zoom-close]') as HTMLButtonElement | null;
  const zoomInButton = dialog?.querySelector('[data-image-zoom-in]') as HTMLButtonElement | null;
  const zoomOutButton = dialog?.querySelector('[data-image-zoom-out]') as HTMLButtonElement | null;
  const zoomLevelLabel = dialog?.querySelector('[data-image-zoom-level]') as HTMLSpanElement | null;

  if (articleRoot && dialog && preview && typeof dialog.showModal === 'function') {
    const zoomableImages = Array.from(articleRoot.querySelectorAll('img')).filter(
      (img) => !img.closest('[data-post-cover]'),
    );
    const MAX_IMAGE_SAMPLE_SIZE = 64;
    const MIN_ZOOM = 1;
    const MAX_ZOOM = 4;
    const ZOOM_STEP = 0.25;
    const DOUBLE_CLICK_ZOOM = 2;
    let zoomLevel = 1;
    let translateX = 0;
    let translateY = 0;
    let activePointerId: number | null = null;
    let dragStartX = 0;
    let dragStartY = 0;
    let pinchStartDistance = 0;
    let pinchStartZoom = MIN_ZOOM;
    let pinchMidpointX = 0;
    let pinchMidpointY = 0;
    const touchPoints = new Map<number, { x: number; y: number }>();
    let rafId: number | null = null;

    const isSvgSource = (source: string) => {
      if (!source) return false;
      if (source.startsWith('data:image/svg+xml')) return true;

      try {
        const resolved = new URL(source, window.location.href);
        return resolved.pathname.toLowerCase().endsWith('.svg');
      } catch {
        return source.toLowerCase().includes('.svg');
      }
    };

    const hasTransparentPixels = (img: HTMLImageElement) => {
      const width = img.naturalWidth;
      const height = img.naturalHeight;
      if (!width || !height) return false;

      const sampleScale = Math.min(1, MAX_IMAGE_SAMPLE_SIZE / Math.max(width, height));
      const sampleWidth = Math.max(1, Math.round(width * sampleScale));
      const sampleHeight = Math.max(1, Math.round(height * sampleScale));
      const canvas = document.createElement('canvas');
      canvas.width = sampleWidth;
      canvas.height = sampleHeight;

      const context = canvas.getContext('2d', { willReadFrequently: true });
      if (!context) return false;

      try {
        context.drawImage(img, 0, 0, sampleWidth, sampleHeight);
        const { data } = context.getImageData(0, 0, sampleWidth, sampleHeight);
        for (let index = 3; index < data.length; index += 4) {
          if (data[index] < 250) {
            return true;
          }
        }
      } catch {
        return false;
      }

      return false;
    };

    const markImagesNeedingSurface = async () => {
      await Promise.allSettled(
        zoomableImages.map(async (img) => {
          try {
            if (!img.complete) {
              await img.decode();
            }
          } catch {
            return;
          }

          const source = img.currentSrc || img.src;
          if (isSvgSource(source) || hasTransparentPixels(img)) {
            img.dataset.imageSurface = 'light';
          } else {
            delete img.dataset.imageSurface;
          }
        }),
      );
    };

    const scheduleRenderZoomLevel = () => {
      if (rafId !== null) return;
      rafId = requestAnimationFrame(() => {
        rafId = null;
        renderZoomLevel();
      });
    };

    const getMaxTranslate = () => {
      const maxTranslateX = Math.max(
        0,
        (preview.clientWidth * zoomLevel - preview.clientWidth) / 2,
      );
      const maxTranslateY = Math.max(
        0,
        (preview.clientHeight * zoomLevel - preview.clientHeight) / 2,
      );
      return {
        x: maxTranslateX,
        y: maxTranslateY,
      };
    };

    const clampTranslate = () => {
      const maxTranslate = getMaxTranslate();
      translateX = Math.max(-maxTranslate.x, Math.min(maxTranslate.x, translateX));
      translateY = Math.max(-maxTranslate.y, Math.min(maxTranslate.y, translateY));
      if (zoomLevel <= MIN_ZOOM) {
        translateX = 0;
        translateY = 0;
      }
    };

    const renderZoomLevel = () => {
      clampTranslate();
      preview.style.transform = `translate(${translateX}px, ${translateY}px) scale(${zoomLevel})`;
      if (zoomLevelLabel) {
        zoomLevelLabel.textContent = `${Math.round(zoomLevel * 100)}%`;
      }
      if (zoomOutButton) {
        zoomOutButton.disabled = zoomLevel <= MIN_ZOOM;
      }
      if (zoomInButton) {
        zoomInButton.disabled = zoomLevel >= MAX_ZOOM;
      }
      if (touchPoints.size === 0) {
        preview.style.cursor = zoomLevel > MIN_ZOOM ? 'grab' : '';
      }
    };

    const setZoomLevel = (nextZoomLevel: number) => {
      zoomLevel = Math.min(MAX_ZOOM, Math.max(MIN_ZOOM, nextZoomLevel));
      renderZoomLevel();
    };

    const setZoomAroundPoint = (
      nextZoomLevel: number,
      originClientX?: number,
      originClientY?: number,
    ) => {
      const prevZoom = zoomLevel;
      zoomLevel = Math.min(MAX_ZOOM, Math.max(MIN_ZOOM, nextZoomLevel));
      if (originClientX !== undefined && originClientY !== undefined && prevZoom !== zoomLevel) {
        const rect = preview.getBoundingClientRect();
        const centerX = rect.left + rect.width / 2;
        const centerY = rect.top + rect.height / 2;
        const ratio = zoomLevel / prevZoom;
        translateX = (translateX - (originClientX - centerX)) * ratio + (originClientX - centerX);
        translateY = (translateY - (originClientY - centerY)) * ratio + (originClientY - centerY);
      }
      renderZoomLevel();
    };

    const openPreview = (source: string, altText: string) => {
      if (!source) return;
      preview.src = source;
      preview.alt = altText;
      translateX = 0;
      translateY = 0;
      setZoomLevel(1);
      dialog.showModal();
      lockBodyScroll();
    };

    const openPreviewFromMermaid = (svg: SVGSVGElement) => {
      const serializedSvg = new XMLSerializer().serializeToString(svg);
      const source = `data:image/svg+xml;charset=utf-8,${encodeURIComponent(serializedSvg)}`;
      openPreview(source, 'Mermaid 图表预览');
    };

    const closeDialog = () => {
      if (!dialog.open) return;
      dialog.close();
      preview.removeAttribute('src');
      preview.alt = '';
      touchPoints.clear();
      activePointerId = null;
      translateX = 0;
      translateY = 0;
      setZoomLevel(1);
    };

    zoomableImages.forEach((img) => {
      img.setAttribute('tabindex', '0');
      img.setAttribute('role', 'button');
      img.setAttribute('aria-label', `点击查看大图：${img.alt || '文章图片'}`);

      const openImagePreview = () => {
        const source = img.currentSrc || img.src;
        openPreview(source, img.alt || '文章图片预览');
      };

      img.addEventListener('click', openImagePreview);
      img.addEventListener('keydown', (event) => {
        if (event.key === 'Enter' || event.key === ' ') {
          event.preventDefault();
          openImagePreview();
        }
      });
    });

    void markImagesNeedingSurface();

    articleRoot.addEventListener('click', (event) => {
      const target = event.target as Element | null;
      const svg = target?.closest('.mermaid-render svg') as SVGSVGElement | null;
      if (!svg) return;
      event.preventDefault();
      openPreviewFromMermaid(svg);
    });

    articleRoot.addEventListener('keydown', (event) => {
      if (event.key !== 'Enter' && event.key !== ' ') return;
      const target = event.target as Element | null;
      const svg = target?.closest('.mermaid-render svg') as SVGSVGElement | null;
      if (!svg) return;
      event.preventDefault();
      openPreviewFromMermaid(svg);
    });

    closeButton?.addEventListener('click', closeDialog);
    zoomInButton?.addEventListener('click', () => {
      setZoomLevel(zoomLevel + ZOOM_STEP);
    });
    zoomOutButton?.addEventListener('click', () => {
      setZoomLevel(zoomLevel - ZOOM_STEP);
    });
    preview.addEventListener(
      'wheel',
      (event) => {
        event.preventDefault();
        const direction = event.deltaY < 0 ? 1 : -1;
        setZoomAroundPoint(zoomLevel + direction * ZOOM_STEP, event.clientX, event.clientY);
      },
      { passive: false },
    );
    preview.addEventListener('dblclick', (event) => {
      event.preventDefault();
      setZoomAroundPoint(
        zoomLevel > MIN_ZOOM ? MIN_ZOOM : DOUBLE_CLICK_ZOOM,
        event.clientX,
        event.clientY,
      );
    });
    preview.addEventListener('dragstart', (event) => {
      event.preventDefault();
    });
    preview.addEventListener('pointerdown', (event) => {
      if (!dialog.open) return;
      preview.setPointerCapture(event.pointerId);
      touchPoints.set(event.pointerId, { x: event.clientX, y: event.clientY });
      preview.style.transition = 'none';

      if (touchPoints.size === 1 && zoomLevel > MIN_ZOOM) {
        activePointerId = event.pointerId;
        dragStartX = event.clientX - translateX;
        dragStartY = event.clientY - translateY;
        preview.style.cursor = 'grabbing';
      }

      if (touchPoints.size === 2) {
        const points = Array.from(touchPoints.values());
        const dx = points[0].x - points[1].x;
        const dy = points[0].y - points[1].y;
        pinchStartDistance = Math.hypot(dx, dy);
        pinchStartZoom = zoomLevel;
        pinchMidpointX = (points[0].x + points[1].x) / 2;
        pinchMidpointY = (points[0].y + points[1].y) / 2;
        activePointerId = null;
        preview.style.cursor = 'grabbing';
      }
    });
    preview.addEventListener('pointermove', (event) => {
      if (!dialog.open || !touchPoints.has(event.pointerId)) return;
      touchPoints.set(event.pointerId, { x: event.clientX, y: event.clientY });

      if (touchPoints.size === 2) {
        event.preventDefault();
        const points = Array.from(touchPoints.values());
        const dx = points[0].x - points[1].x;
        const dy = points[0].y - points[1].y;
        const nextDistance = Math.hypot(dx, dy);
        if (pinchStartDistance > 0) {
          const zoomRatio = nextDistance / pinchStartDistance;
          const prevZoom = zoomLevel;
          zoomLevel = Math.min(MAX_ZOOM, Math.max(MIN_ZOOM, pinchStartZoom * zoomRatio));
          if (prevZoom !== zoomLevel) {
            const k = zoomLevel / prevZoom;
            const rect = preview.getBoundingClientRect();
            const cx = rect.left + rect.width / 2;
            const cy = rect.top + rect.height / 2;
            translateX = (translateX - (pinchMidpointX - cx)) * k + (pinchMidpointX - cx);
            translateY = (translateY - (pinchMidpointY - cy)) * k + (pinchMidpointY - cy);
          }
          scheduleRenderZoomLevel();
        }
        return;
      }

      if (activePointerId === event.pointerId && zoomLevel > MIN_ZOOM) {
        event.preventDefault();
        translateX = event.clientX - dragStartX;
        translateY = event.clientY - dragStartY;
        scheduleRenderZoomLevel();
      }
    });

    const resetPointer = (event: PointerEvent) => {
      if (preview.hasPointerCapture(event.pointerId)) {
        preview.releasePointerCapture(event.pointerId);
      }
      touchPoints.delete(event.pointerId);

      if (activePointerId === event.pointerId) {
        activePointerId = null;
      }

      if (touchPoints.size === 0) {
        preview.style.transition = '';
        preview.style.cursor = zoomLevel > MIN_ZOOM ? 'grab' : '';
      }

      if (touchPoints.size === 1 && zoomLevel > MIN_ZOOM) {
        const [remainingPointerId, remainingPoint] = Array.from(touchPoints.entries())[0];
        activePointerId = remainingPointerId;
        dragStartX = remainingPoint.x - translateX;
        dragStartY = remainingPoint.y - translateY;
        preview.style.cursor = 'grabbing';
      }
    };

    preview.addEventListener('pointerup', resetPointer);
    preview.addEventListener('pointercancel', resetPointer);
    dialog.addEventListener('click', (event) => {
      if (event.target === dialog) {
        closeDialog();
      }
    });
    dialog.addEventListener('keydown', (event) => {
      if (!dialog.open) return;
      if (event.key === '+' || event.key === '=') {
        event.preventDefault();
        setZoomLevel(zoomLevel + ZOOM_STEP);
      }
      if (event.key === '-' || event.key === '_') {
        event.preventDefault();
        setZoomLevel(zoomLevel - ZOOM_STEP);
      }
      if (event.key === '0') {
        event.preventDefault();
        setZoomLevel(1);
      }
    });
    dialog.addEventListener('cancel', () => {
      preview.removeAttribute('src');
      preview.alt = '';
      touchPoints.clear();
      activePointerId = null;
      translateX = 0;
      translateY = 0;
      setZoomLevel(1);
    });
    dialog.addEventListener('close', () => {
      unlockBodyScroll();
      preview.style.transition = '';
      preview.style.cursor = '';
    });
    dialog.addEventListener(
      'wheel',
      (event) => {
        event.preventDefault();
      },
      { passive: false },
    );
    dialog.addEventListener(
      'touchmove',
      (event) => {
        if (event.target === dialog) {
          event.preventDefault();
        }
      },
      { passive: false },
    );

    renderZoomLevel();
  }
</script>
